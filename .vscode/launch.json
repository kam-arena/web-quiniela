{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Start",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8000/index.html",
      "webRoot": "${workspaceFolder}",
      "runtimeArgs": ["--new-window"],
      "preLaunchTask": "Restart Python Server"
    }
  ]
}